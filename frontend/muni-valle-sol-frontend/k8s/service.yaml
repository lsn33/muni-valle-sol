apiVersion: v1
kind: Service
metadata:
  name: muni-frontend
  namespace: muni-valle-sol
  labels:
    app: muni-frontend
spec:
  selector:
    app: muni-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: LoadBalancer
